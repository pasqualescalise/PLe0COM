VAR result : int;

PROCEDURE f1(x, y : int);
	VAR result, ret : int;

	PROCEDURE f2(x, y : int) -> (int);
		VAR space : int;

		PROCEDURE f3(x, y : int);
			BEGIN
				result := x + x
			END;

		PROCEDURE f4(x, y : int);
			VAR space : int;
			BEGIN
				result := result + x
			END;

		BEGIN
			result := x * y;
			print result;
			CALL f3(x, y);
			print result;
			CALL f4(x, y);
			print result;
			return (0)
		END;

	BEGIN
		result := x + y;
		CALL f2(x, y) -> (ret);
		print result;
		print ret
	END;

BEGIN
	CALL f1(3, 4)
END.
